[package]
name = "lir"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bir = { path = "../bir/" }
sema = { path = "../sema/" }
utils = { path = "../utils" }
smallvec = "1.10"
