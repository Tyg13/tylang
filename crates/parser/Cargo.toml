[package]
name = "parser"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
cst = { path = "../cst/" }
utils = { path = "../utils/" }

[dev-dependencies]
expect-test = "1.2.2"
